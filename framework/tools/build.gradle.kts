plugins {
    id("smschecker.android.library")
    id("smschecker.android.core")
    id("smschecker.koin")
    id("smschecker.detekt")
}

android {
    namespace = "com.sms.checker.forwarder.framework.tools"
}